# Real-project reference datasets

This directory contains evidence manifests only. Original customer/project files are
kept outside the repository and are identified by filename, byte size and SHA-256.

A manifest is evidence of which files were used. It is not evidence that every format
was fully parsed or that a project package is complete.

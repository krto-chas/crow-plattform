# Archive

Superseded material retained unchanged, for historical traceability only. Never referenced normatively.

- `original-caf-rc1/` — the original CAF RC1 corpus (markdown only; the .docx copies were deleted per the single-source rule). The duplicate identifiers CAS-906–910 are unambiguous here through their distinct filenames (e.g. `CAS-906_Federated_Cognitive_Architecture` vs `CAS-906_Federation_Identity_Architecture`); identifiers CAS-905–943 must not be reused.
- `uml-v0.1.0/` — PlantUML sources and index of the UML package whose diagrams trace to the archived CAS series. Rendered PNG/SVG outputs were not retained (generated artefacts). Current diagrams of the actual system live in `../../00_Architecture/DIAGRAMS.md` as Mermaid.

See `../MIGRATION_FROM_RC1.md` for the full mapping.
